{
  "name": "local-dev-server",
  "version": "1.0.0",
  "description": "Local dev server",
  "main": "index.js",
  "author": "nkruk@salesforce.com",
  "license": "MIT",
  "bin": {
    "localdevtools": "./bin/localdevtools.js"
  },
  "scripts": {
    "clean": "yarn rimraf dist",
    "build": "tsc -p .",
    "watch": "tsc -w | awk '{gsub(/\\033c/,\"\") system(\"\")}1'",
    "lint": "yarn eslint src --ext js,ts",
    "lint:ci": "yarn lint --output-file ./reports/eslint.log --no-color",
    "start": "node dist/index.js",
    "debug": "node --inspect-brk dist/index.js",
    "test": "yarn jest src --passWithNoTests --runInBand",
    "test:debug": "node --inspect node_modules/.bin/jest src --runInBand",
    "test:ci": "yarn jest --maxWorkers=1 --ci --config jest.config.all.js",
    "format": "prettier --write \"src/**/*.ts\"",
    "precommit": "lint-staged",
    "prepush": "yarn prepare && yarn lint && yarn test"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@typescript-eslint/eslint-plugin": "^1.6.0",
    "@typescript-eslint/parser": "^1.6.0",
    "eslint": "^5.16.0",
    "eslint-plugin-prettier": "^3.0.1",
    "eslint-config-prettier": "^4.1.0",
    "husky": "^1.3.1",
    "jest": "^24.7.1",
    "jest-runner-eslint": "^0.7.3",
    "lint-staged": "^8.1.5",
    "prettier": "^1.16.4",
    "rimraf": "^2.6.3",
    "ts-jest": "24.0.2",
    "ts-node": "^8.0.3",
    "typescript": "^3.4.3"
  },
  "dependencies": {
    "@lwc/compiler": "^0.34.7",
    "express-http-proxy": "^1.5.1",
    "gulp": "^4.0.0",
    "memory-cache": "^0.2.0",
    "morgan": "^1.9.1",
    "object-hash": "^1.3.1",
    "local-dev-server": "0.0.1"
  },
  "engines": {
    "node": "10.15.3"
  }
}
