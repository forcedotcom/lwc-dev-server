describe('LocalDevServerConfiguration', () => {
    it.todo(
        'when you do not pass anything to the constructor, it proceeds without error'
    );

    it.todo(
        'when you pass an invalid file to the constructor, it notifies you that the file was not valid.'
    );

    it.todo(
        'when you pass a file that is not json to the constructor, it notifies you that the file was an invalid syntax.'
    );
});
